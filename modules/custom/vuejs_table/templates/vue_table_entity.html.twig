{#
/**
 * @file vue_table_entity.html.twig
 * Default theme implementation to present Vue table entity data.
 *
 * This template is used when viewing Vue table entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_vue_table_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('vue_table_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
